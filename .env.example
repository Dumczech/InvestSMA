# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=

# Resend (lead capture notifications). If RESEND_API_KEY is unset, the
# /api/leads endpoint still captures the lead but skips the email step.
RESEND_API_KEY=
LEAD_NOTIFY_TO=justin@luxrentalmgmt.com
LEAD_NOTIFY_FROM=InvestSMA <leads@investsma.com>
